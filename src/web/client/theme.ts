// Shared by the <head> script in HtmlLayout and the bundle: both must agree on
// the key and the daisyUI theme names, or the page flashes the wrong theme.
export const THEME_KEY = "web-pi:theme";
export const LIGHT_THEME = "piweb";
export const DARK_THEME = "piweb-dark";
